<Window x:Class="Engine.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Engine"
        mc:Ignorable="d"
        WindowState="Maximized"
        WindowStyle="None"
        ResizeMode="NoResize"
        Title="DrwalCraft" Height="450" Width="800">
    <Grid Background="#222">
        <!-- GŁÓWNY PODZIAŁ: MENU | MAPA -->
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="272"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- ================= MENU ================= -->
        <Grid Grid.Column="0"
            Background="#222">

            <!-- MINIMAPA (GÓRA) + RESZTA MENU (DÓŁ) -->
            <Grid.RowDefinitions>
                <RowDefinition Height="272"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- MINIMAPA -->
            <Border Grid.Row="0"
                    Margin="7"
                    BorderThickness="1"
                    BorderBrush="#555">
                <Image x:Name="MiniMapImage"
                    Stretch="Uniform"
                    Width="256"
                    Height="256"/>
            </Border>

            <!-- PANEL MENU / INFO O ZAZNACZONYM OBIEKCIE -->
            <StackPanel Grid.Row="1"
                        Margin="8"
                        Orientation="Vertical">

                <!-- przykładowe elementy -->
                <TextBlock Text="Selected unit"
                        Foreground="White"
                        FontWeight="Bold"
                        Margin="0,0,0,8"/>

                <TextBlock x:Name="UnitNameText"
                        Foreground="White"/>

                <ProgressBar x:Name="HealthBar"
                            Height="16"
                            Margin="0,8,0,0"/>

                <!-- reszta UI -->
            </StackPanel>
        </Grid>

        <!-- ================= MAPA GRY ================= -->
        <Grid x:Name="GameMapGrid"
            Grid.Column="1"
            Background="#3A7F3A"
            ClipToBounds="True">
            <Image x:Name="GameMapImage"
                Stretch="Fill"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                MouseWheel="MainMapMouseWheel"/>
        </Grid>

</Grid>

</Window>
